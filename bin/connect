#!/bin/bash

if [ -r "/home/logplex/keys.sh" ]; then
    . /home/logplex/keys.sh
fi

NODENAME=$1

if [ -z "${NODENAME}" ]; then
    NODENAME=remsh
fi

erl -name ${NODENAME}@$LOCAL_IP -hidden -setcookie $LOGPLEX_COOKIE -remsh logplex@$LOCAL_IP
